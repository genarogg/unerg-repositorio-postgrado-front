"use client"
import type React from "react"

type FiltersProps = {}

const Filters: React.FC<FiltersProps> = () => {
  return <>{/* Solo mantenemos la búsqueda, removemos FromToDate y SelectFilters */}</>
}

export default Filters
